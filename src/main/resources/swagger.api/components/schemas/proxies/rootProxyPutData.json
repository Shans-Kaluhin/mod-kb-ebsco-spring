{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Root Proxy Put Data Schema",
  "description": "Root Proxy Put Data Schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Root Proxy Id",
      "enum": ["root-proxy"],
      "example": "root-proxy"
    },
    "type": {
      "type": "string",
      "description": "Root Proxy Type",
      "enum": ["rootProxies"],
      "example": "rootProxies"
    },
    "attributes": {
      "type": "object",
      "description": "Root Proxy Data Attributes",
      "$ref": "rootProxyDataAttributes.json"
    }
  },
  "required": [
    "id",
    "type",
    "attributes"
  ]
}
